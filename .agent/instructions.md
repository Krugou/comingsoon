# Auto-Sync Instruction

Always check for remote changes and sync the repository at the start of every session to ensure work is performed on the latest code.

1. Run `git fetch`
2. Run `git status`
3. If behind, run `git pull` (stashing local changes if necessary)
